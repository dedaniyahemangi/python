# calculate the length of a string.

str=input("Enter String: ")
count=0
for i in str:
    count=count+1
print("Length of the string is:", count)

# we can find length without writing any code with just print
# print(len(str))